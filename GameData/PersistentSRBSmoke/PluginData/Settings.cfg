PERSISTENT_SRB_SMOKE
{
    enabled = true

    // Performance
    maxParticles = 16000
    maxEmitPerFrame = 96

    // Lifetime and trail continuity
    lifetime = 150
    baseEmissionRate = 24
    particlesPerMeter = 0.22
    teleportDistance = 750

    // Visual appearance
    startSize = 2.8
    sizeGrowth = 9.0
    opacity = 0.72

    // Motion
    driftSpeed = 0.75
    buoyancy = 0.18
    turbulenceStrength = 0.65
    turbulenceFrequency = 0.08

    debugLogging = false
}
